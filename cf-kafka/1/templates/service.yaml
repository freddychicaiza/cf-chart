kind: Service
apiVersion: v1
metadata:
  name: "{{ .Values.name }}"
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.name }}
  ports:
  - protocol: TCP
    port: 2181
    targetPort: 2181
  type: ClusterIP
status:
  loadBalancer: {}
